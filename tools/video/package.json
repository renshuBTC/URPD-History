{
  "name": "urpd-video",
  "private": true,
  "type": "module",
  "description": "Renders the full-history 4K video that the site's download button serves (see render.mjs). Not part of the site, which has no build step.",
  "dependencies": {
    "@fontsource/jetbrains-mono": "5.3.0",
    "playwright": "1.47.2",
    "plotly.js-dist-min": "2.27.0"
  }
}
